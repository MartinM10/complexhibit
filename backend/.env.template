# ===========================================
# COMPLEXHIBIT BACKEND - Environment Template
# ===========================================
# Copy this file to .env and fill in the values

# -----------------------------
# Virtuoso / Knowledge Graph
# -----------------------------
URI_ONTOLOGIA=https://w3id.org/OntoExhibit#
VIRTUOSO_URL=http://localhost:8890/sparql
DEFAULT_GRAPH_URL=https://w3id.org/OntoExhibit/Data

# -----------------------------
# PostgreSQL (Auth Database)
# -----------------------------
POSTGRES_USER=complexhibit
POSTGRES_PASSWORD=complexhibit
POSTGRES_DB=complexhibit_auth

# -----------------------------
# JWT Authentication
# -----------------------------
# IMPORTANT: Generate a secure secret for production!
# Run: python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=change_this_in_production

# -----------------------------
# SMTP Email Configuration
# -----------------------------
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password
SMTP_FROM=your_email@gmail.com

# -----------------------------
# Admin Settings
# -----------------------------
ADMIN_EMAIL=admin@example.com

# -----------------------------
# Frontend URL (for email links)
# -----------------------------
# Change this to your production frontend URL
FRONTEND_URL=http://localhost:3000

# ===========================================
# LEGACY VARIABLES (can be removed)
# ===========================================
# These are kept for backwards compatibility but not actively used:
# - DATABASE_STARDOG, ENDPOINT_STARDOG, USERNAME_STARDOG, PASSWORD_STARDOG (use Virtuoso instead)
# - DJANGO_SECRET_KEY (replaced by JWT_SECRET)
# - USER_SERVICE_URL (legacy Django service)
# - DEPLOY_PATH (configured in docker-compose)